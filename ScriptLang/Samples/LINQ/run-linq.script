
let testReturnLoop = () => {
    var sum = 0
    for i in range(1, 10) {
        if (i == 5) {
            return sum  // 遇到 5 立即返回
        }
        sum = sum + i
    }
    return -1 // 循环结束也返回 -1
}

print("testReturnLoop:", testReturnLoop()) 