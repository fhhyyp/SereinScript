print("=== 深层递归测试 ===")

let fib = (n) => {
    if n <= 1 then {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

let start = now()
let result = fib(35)
let end = now()
print("fib(35) = " + result)
print("耗时: " + (end - start) / 10000 + "ms")