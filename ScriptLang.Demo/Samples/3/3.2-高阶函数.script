
// 高阶函数
let map = (arr, fn) => {
    var result = []
    for item in arr {
        result = result + [fn(item)]
    }
    result
}

let squares = map([1, 2, 3, 4], x => x * x + 100)
print("squares:", squares)  // 输出: squares: [1, 4, 9, 16]